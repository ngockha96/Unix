#include <string.h>
#include <stdio.h>

using namespace std;
	
int main (void)
	{
		string u_foldername,u_filename;
		cout << "Which folder do want to find ?";
		getline (cin,u_foldername);
		
		cout << "Type in the name of the file.";
		getline (cin,u_filename);
	}